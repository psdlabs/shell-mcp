name: Publish to npm

on:
  release:
    types: [published]

jobs:
  publish:
    name: Publish packages to npm
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - run: pnpm install --frozen-lockfile

      - run: pnpm build

      # Publish core first (dependency of mcp-lite)
      - name: Publish @shell-mcp/core
        run: pnpm --filter @shell-mcp/core publish --no-git-checks --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      # Then publish mcp-lite (the main package people install)
      - name: Publish @shell-mcp/mcp-lite
        run: pnpm --filter @shell-mcp/mcp-lite publish --no-git-checks --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
